= form_for(@page, class: "form-horizontal") do |f|
	= render 'shared/error_messages', object: f.object
	.field
		.form-group
			-#= f.label :title, "Title"
			= f.text_field :title,	placeholder: "Title", class: "form-control"
		.form-group
			= f.text_area :content, placeholder: "Write a new post...", class: "form-control", rows: 20
		.form-group
			= f.submit "Submit", class: "btn btn-large btn-primary"
			%button.btn(type = "button" data-toggle="modal" data-target="#formatting_help")
				Formatting help
			.pull-right
				.checkbox-inline
					:erb
						<%= f.check_box :sequence%> Sequence
				.checkbox-inline
					:erb		
						<%= f.check_box :public%> Public
- formatting = Page.find_by(title: "Formatting help")
.modal.fade#formatting_help(tabindex = "-1" role = "dialog" aria-labelledby = "formatting_help_screen" aria-hidden="true")
	.modal-dialog
		.modal-content
			.modal-header
				%button.close(type = "button" data-dismiss = "modal" aria-hidden="true") &times;

				%h4 Formatting Help
			.modal-body
				- if formatting
					!~ kramdown formatting.content
				- else
					:markdown
						Uses [kramdown](http://kramdown.gettalong.org/syntax.html), a super-set of markdown. 
			.modal-footer
				%button.btn.pull-left(type = "button" data-dismiss = "modal") Close

