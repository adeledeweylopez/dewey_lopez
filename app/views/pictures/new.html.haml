
= form_for(@picture) do |f|
	.field
		.form-group
			= f.label :photo, "Photo", class: "form-control" 
			= f.file_field :photo, class: "form-control"
		
		.form-group
			= f.submit "Upload", class: "btn btn-large btn-primary"
